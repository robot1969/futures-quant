# 🔧 GUI 启动失败修复指南

## 问题诊断

运行诊断工具快速定位问题：

```bash
cd ~/futures_quant
source venv/bin/activate
python gui_diagnose.py
```

## 常见问题及解决方案

### ❌ 问题 1: matplotlib 缺失

**错误信息**: `No module named 'matplotlib'`

**解决方案**:
```bash
source venv/bin/activate
pip install matplotlib plotly
```

### ❌ 问题 2: 数据目录为空

**错误信息**: `数据目录为空` 或 `未找到任何合约数据`

**解决方案**:
```bash
# 先运行命令行版本生成数据
python main.py

# 然后再启动 GUI
python main.py --gui
```

### ❌ 问题 3: tkinter 不可用

**错误信息**: `No module named 'tkinter'` 或相关错误

**解决方案**:

**macOS**:
```bash
# Python 3.10+ 通常已包含 tkinter
# 如果确实缺失，重新安装 Python
brew install python-tk
```

**Linux (Ubuntu/Debian)**:
```bash
sudo apt-get install python3-tk
```

**Windows**:
```bash
# 重新运行 Python 安装程序
# 确保勾选 "tcl/tk and IDLE" 选项
```

### ❌ 问题 4: 虚拟环境未激活

**错误信息**: 各种模块缺失错误

**解决方案**:
```bash
# 激活虚拟环境
source venv/bin/activate

# 如果 venv 不存在，创建它
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### ❌ 问题 5: 依赖包版本不兼容

**错误信息**: 导入错误或属性错误

**解决方案**:
```bash
# 更新所有依赖
pip install --upgrade -r requirements.txt

# 或重新安装
pip uninstall -y pandas numpy matplotlib plotly
pip install -r requirements.txt
```

## 完整修复流程

### 方案 A: 快速修复（推荐）

```bash
cd ~/futures_quant

# 1. 激活虚拟环境
source venv/bin/activate

# 2. 安装缺失的依赖
pip install matplotlib plotly

# 3. 生成数据
python main.py

# 4. 启动 GUI
python main.py --gui
```

### 方案 B: 完整重装

```bash
cd ~/futures_quant

# 1. 删除旧的虚拟环境
rm -rf venv

# 2. 创建新的虚拟环境
python -m venv venv
source venv/bin/activate

# 3. 安装所有依赖
pip install --upgrade pip
pip install -r requirements.txt

# 4. 生成数据
python main.py

# 5. 启动 GUI
python main.py --gui
```

## 验证修复

运行诊断工具确认所有检查通过：

```bash
python gui_diagnose.py
```

应该看到：
```
总计：12/12 通过

🎉 所有检查通过！GUI 应该可以正常运行。
```

## 启动 GUI

修复完成后，使用以下任一方式启动：

### 方式 1: 命令行参数
```bash
python main.py --gui
```

### 方式 2: 启动脚本
```bash
./start_gui.sh
```

### 方式 3: 直接运行
```bash
python gui/dashboard.py
```

## 仍然失败？

如果上述方案都无法解决问题：

1. **查看详细错误日志**:
   ```bash
   python main.py --gui 2>&1 | tee gui_error.log
   ```

2. **检查 Python 版本**:
   ```bash
   python --version
   # 应该是 3.10+
   ```

3. **检查 tkinter**:
   ```bash
   python -m tkinter
   # 应该打开一个示例窗口
   ```

4. **提交 Issue**:
   将错误日志和诊断结果提交到 GitHub 仓库

---

**🦞 祝修复顺利！**
